{
  "name": "gitbook-plugin-jquery-book",
  "version": "0.0.1",
  "description": "jQuery plugin for my book",
  "engines": {
    "gitbook": ">1.x.x"
  },
  "gitbook": {
    "properties": {
      "jquery": {
        "type": "string",
        "default": "it's the default value",
        "description": "It defines my awesome config!"
      }
    }
  }
}